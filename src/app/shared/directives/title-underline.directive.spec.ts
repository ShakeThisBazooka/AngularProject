import { TitleUnderlineDirective } from './title-underline.directive';

describe('TitleUnderlineDirective', () => {
  /* it('should create an instance', () => {
    const directive = new TitleUnderlineDirective();
    expect(directive).toBeTruthy();
  }); */
});
